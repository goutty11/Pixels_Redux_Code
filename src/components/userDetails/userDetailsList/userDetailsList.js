import React from 'react';
import './PostsList.css';
i
const UserList = (props) => {
    return (
        <div>
            {
                props.topicData.mail_id
            }
        </div>
    );
};

export default UserList;